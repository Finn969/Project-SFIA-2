---
- name: Install Docker
  hosts: servers
  become: true
  roles:
    - docker-installation

- name: Initialise Docker Swarm
  hosts: swarm-manager
  become: true
  roles:
    - docker-swarm-init

- name: add workers
  hosts: swarm-workers
  become: true
  roles:
    - docker-swarm-add-workers
  