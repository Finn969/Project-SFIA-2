---
- name: Install Docker
  hosts: local
  become: true
  roles:
    - docker-installation

- name: Initialise Docker Swarm
  hosts: local
  become: true
  roles:
    - docker-swarm-init

# Unable to implement worker nodes
#- name: add workers
#  hosts: swarm-workers
#  become: true
#  roles:
#    - docker-swarm-add-workers
  